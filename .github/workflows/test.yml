name: Test

on:
  [push]

jobs:

  build:
   runs-on: ubuntu-latest
   steps:
    - uses: actions/checkout@v2
    - name: Migrate Database & Execute Tests
      run: php artisan migrate:fresh --seed
    - name: Run tests
      run: php artisan test
